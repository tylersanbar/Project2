Readme - Project 2 - Tyler Sanbar

PosAvg
String fileName = "Mesonet.txt";
ArrayList<MesoStation> stationList; populated from read()
int index;

PosAvg(stID);
read(); fills stationList from fileName;
indexOfStation(); calculates the file index of the station and stores in int index
toString(); returns "This index is average of <station-1> and <station+1>
 , <station-2> and <station+2>, and so on." where <station+-n> is the station at the index +-n
 
MesoInherit
String fileName = "Mesonet.txt";
ArrayList<MesoStation> stationList; populated from read()
int[] avgList = new int[3]; array index [0] stores ceiling, [1] stores floor, [2] stores average 

MesoInherit(MesoStation station(stid)); station parameter is given manually
read(); fills stationList from fileName;
int[] calAverage(); populates avgList[]
numberOfStationWithLetterAvg();
letterAverage() returns char with ASCII value == calAverage()[2]
 
LetterAvg
String fileName = "Mesonet.txt";
ArrayList<MesoStation> stationList; populated from read()
ArrayList<MesoStation> sameAvg; stores list of stations with the same letter avg as ltrAvg

LetterAvg(int ltrAvg); output uses mesoInherit.letterAverage() as parameter
read(); fills stationList from fileName;
numberOfStationWithLetterAvg() returns the size of sameAvg()[]
toString(); "They are: /n<First>/n<Second>/n<ect...>" <> is list of elements in sameAvg()[]
